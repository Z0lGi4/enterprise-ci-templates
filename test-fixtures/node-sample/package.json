{
  "name": "sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . && tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "vitest": "^5.0.0"
  }
}
